``` Task description:

- Read additional resources
- Create a TS project
- Take the DZ for lesson 5 (Functions) as a basis, namely the files of regular and arrow functions (we do not take the file with getters and setters) and rework them so that it is TS code, that is, add typing to it.```